class Solution {
    public boolean isPalindrome(int x) {
        int y=x;
        int sum=0;
        while(y>0)
        { sum=sum*10+(y%10);
        y/=10;

        }
     return (sum==x)? true:false;
    }
}
